import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Buat Undangan Baru",
};

export default function NewInvitationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
